# Copy this file to .env and fill in real values before running docker compose up.
# Generate a random Jwt key with: openssl rand -base64 48
JWT_KEY=
JWT_ISSUER=TutorTrackApi
JWT_AUDIENCE=TutorTrackAngular
JWT_EXPIRY_MINUTES=10080

AUTH_USERNAME=
AUTH_PASSWORD=
